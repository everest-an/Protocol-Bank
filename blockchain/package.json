{
  "name": "protocol-bank-blockchain",
  "version": "1.0.0",
  "description": "Smart contracts for Protocol Bank's global clearing network",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy-clearinghouse.js --network sepolia",
    "deploy:mainnet": "hardhat run scripts/deploy-clearinghouse.js --network mainnet",
    "verify": "hardhat verify",
    "node": "hardhat node"
  },
  "keywords": [
    "ethereum",
    "blockchain",
    "clearing",
    "settlement",
    "defi"
  ],
  "author": "Protocol Bank",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1",
    "ethers": "^6.9.0"
  }
}
